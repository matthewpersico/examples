set | sort > $(basename $0).env
scoping_func inner
echo -n "inner:in inner dollarZero $0 vs bashsource ("
for i in $(seq 1 ${#BASH_SOURCE[@]})
do
ii=$((i-1))
echo -n " [$ii] ${BASH_SOURCE[$ii]}"
done
echo ' )'